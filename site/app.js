const state={hub:null,view:'home',query:''};
const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function boot(){try{state.hub=await fetch('site-data/hub.json',{cache:'no-store'}).then(r=>{if(!r.ok)throw Error(r.status);return r.json()});$('#status').textContent=`${state.hub.counts.repositories} repos • ${new Date(state.hub.generated_at).toLocaleString()}`;renderTree();render()}catch(e){$('#status').textContent='data unavailable';$('#content').innerHTML=`<h1 class="title">Command Site</h1><p class="muted">No aggregate data has been deployed yet.</p>`}}
function groups(){return [['Repositories','repositories'],['Tools','tools'],['Toolsets','toolsets'],['Cases','cases'],['Opportunities','opportunities'],['Intelligence','intelligence'],['Sources','sources'],['Activity','activity']]}
function renderTree(){let h='';for(const[g,k]of groups()){const a=state.hub[k]||[];h+=`<div class="tree-group"><div class="tree-title">${g}<span class="badge">${a.length}</span></div>`;for(const x of a.slice(0,50)){const label=x.name||x.title||x.full_name||x.id||'item';h+=`<div class="tree-item" data-kind="${k}" data-id="${esc(x.id||x.repo_id||x.full_name||label)}">${esc(label)}</div>`}h+='</div>'}$('#tree').innerHTML=h;document.querySelectorAll('.tree-item').forEach(el=>el.onclick=()=>detail(el.dataset.kind,el.dataset.id))}
function filtered(a){const q=state.query.toLowerCase();return !q?a:a.filter(x=>JSON.stringify(x).toLowerCase().includes(q))}
function render(){const h=state.hub;if(state.view==='home'){const c=h.counts;$('#crumb').textContent='COMMAND / HOME';$('#content').innerHTML=`<h1 class="title">Multi-Repo Command Center</h1><div class="grid">${Object.entries(c).map(([k,v])=>`<div class="card"><div class="k">${esc(k)}</div><div class="n" style="font-size:28px">${v}</div></div>`).join('')}</div><h2>Repositories</h2><div class="grid">${h.repositories.map(repoCard).join('')||'<p class="muted">No repositories connected yet.</p>'}</div>`;document.querySelectorAll('[data-repo]').forEach(e=>e.onclick=()=>repoView(e.dataset.repo));return}listView(state.view)}
function repoCard(r){return `<div class="card" data-repo="${esc(r.id)}"><b>${esc(r.full_name)}</b><p class="muted">${esc(r.description||'Connected repository')}</p><span class="pill">${esc(r.default_branch)}</span><span class="pill">${esc((r.source_commit||'').slice(0,8))}</span></div>`}
function listView(k){const a=filtered(state.hub[k]||[]);$('#crumb').textContent=`COMMAND / ${k.toUpperCase()}`;$('#content').innerHTML=`<h1 class="title">${esc(k)}</h1>${a.map(x=>`<div class="row"><b>${esc(x.name||x.title||x.full_name||x.id||'item')}</b><div class="muted">${esc(x.repo_full_name||x.url||x.path||'')}</div></div>`).join('')||'<p class="muted">Nothing here yet.</p>'}`}
function repoView(id){const r=state.hub.repositories.find(x=>x.id===id);if(!r)return;$('#crumb').textContent=`COMMAND / REPOS / ${r.full_name}`;const sections=groups().slice(1).map(([label,k])=>{const n=(state.hub[k]||[]).filter(x=>x.repo_id===id).length;return `<span class="pill">${label}: ${n}</span>`}).join('');$('#content').innerHTML=`<h1 class="title">${esc(r.full_name)}</h1><p>${esc(r.description||'')}</p><p><a href="${esc(r.url)}" target="_blank">Open on GitHub</a></p>${sections}<pre>${esc(JSON.stringify(r.stats||{},null,2))}</pre>`}
function detail(kind,id){const a=state.hub[kind]||[];const x=a.find(v=>(v.id||v.repo_id||v.full_name||v.name||v.title)===id);if(!x)return;$('#crumb').textContent=`COMMAND / ${kind.toUpperCase()} / DETAIL`;$('#content').innerHTML=`<h1 class="title">${esc(x.name||x.title||x.full_name||x.id||'Detail')}</h1><pre>${esc(JSON.stringify(x,null,2))}</pre>`}
document.querySelectorAll('.activity button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.activity button').forEach(x=>x.classList.remove('active'));b.classList.add('active');state.view=b.dataset.view;render()});$('#search').oninput=e=>{state.query=e.target.value;render()};boot();
